import java.util.ArrayList;

public class ErrorInArrayList {
    public static void main(String [] args){
         /*d:
        ArrayList<Integer> values = new ArrayList<Integer>();

        for (int i = 1; i <= 10; i++)
        {
            values.set(i - 1, i * i);
        }
          */

        /*e: need to initialize
        ArrayList<Integer> values;

        for (int i = 1; i <= 10; i++)
        {
            values.add(i * i);
        }
         */
    }
}
